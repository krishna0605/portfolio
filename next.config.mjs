/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // reactCompiler is not supported in stable Next 14 without experimental flags, removing for stability on Node 18
};

export default nextConfig;
